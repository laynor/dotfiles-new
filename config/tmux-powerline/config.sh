# tmux-powerline user config. Sourced before defaults are applied.
# Refresh interval (seconds). Default of 1 spawns a bash+nu segment tree
# per window every second -> heavy process churn. 15 is tmux's own default.
export TMUX_POWERLINE_STATUS_INTERVAL=15
